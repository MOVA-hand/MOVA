回顾我们之前对attention可视化部分的逻辑和操作，继续进行这部分的逻辑分析与实验代码实现。
关于实验的设计，由于我们这里是点云做query，图像做key/value，所以我想一个的attn对整个手的高斯点云的可视化。我想在这里做一个对每个点的global reliance map。具体来说，我们需要对每个点token计算global的信息，然后映射到高斯的颜色上着色，通过在3D高斯手上热力图的形式，观察哪些区域更依赖global token。类似地，再对每个点做局部attn entropy map。
还有一点，我们要么做所有head的平均，要么取最后一个head，不要第一个头或者中间值这种。
最重要的是，由于我们使用的是VET而不是32*32的图像token，所以在对于采样点做attn_map的时候，应该不能直接reshape回图像吧？应该是做attention composition对吗？你现在的对采样点的可视化还停留在VET层面对吗？应该返回到image层面才能看到attention的有效性是吗？

基于我的代码，深入挖掘了解逻辑，并思考上面的内容，先分析不用修改代码，我们确定之后再改代码


############################################################################

1. 先默认用最后一个头
2. 对于local的，当然是只算1024个local token
3. forward_transformer中的merge_token是作为了后面的1024个token，这里的实际调用是 modeling_hand_lrm.py 中的1859-1874行的逻辑，也就是说这里输入的transformer中的key/value（我们称之为visual evidence token，VET）是先将变形的点映射对齐图像32*32的token，然后采样聚合，最终通过adapter压缩回1024个token的构造，因此早已经不是image token32*32的形式了

############################################################################

好的，先进行第一步的代码增加，这一步有几个需要分析和考虑的点。
这一步需要增加的代码逻辑较多，为了防止原代码被污染，请新增开关接口，防止直接在原代码上修改。首先我们attn部分的高斯点的数量是12337个，而后面又在网格面上增加点数量到36945。在做这个point-wise attention的可视化时，都需要只渲染这12337个点，我目前已经把这些增加的点注释起来了。需要注意的是，我们需要先计算这些数值，然后归一化在将这些值覆盖高斯的原始颜色，下面给一个高斯密度的热力图的逻辑供你参考，修改完之后debug运行确保能跑通且逻辑正确
            # HEAT MAP AS COLOR
            """
            xyz = predicted_parameters["gaussians_xyz"]
            dist_sq, nn_ind, _ = knn_points(xyz, xyz, K=6, return_nn=False)
            dist_sq = torch.mean(dist_sq, -1)
            min_val = 0.0000
            max_val = 0.0003
            dist_sq = torch.clip(dist_sq, min_val, max_val)
            dist_sq = (dist_sq - min_val) / (max_val - min_val)
            dist_sq = dist_sq.unsqueeze(-1)
            dist_sq = dist_sq.repeat(1, 1, 3)
            dist_sq = dist_sq.detach().cpu().numpy()
            dist_sq = cv2.applyColorMap((dist_sq[:, :, 0] * 255).astype(np.uint8), cv2.COLORMAP_JET) / 255
            dist_sq = torch.FloatTensor(dist_sq).to(self.device)
            predicted_parameters["gaussians_colors"] = torch.nn.Parameter(dist_sq, requires_grad=True)
            """

############################################################################

直接跑一个完整的可视化调试，原始的测试代码逻辑是根据输入的一张图片到预训练的模型中，然后变形到多个姿势上测试渲染效果，但是现在的attention可视化不需要变形到多个姿势测试效果，只需要在当前输入图片的姿势和视角下即可，为了看到不可见区域的可视化，需要在当前姿势的跟关节变形让手绕x轴渲染90度 （np.array([-np.pi/2, 0, 0])）

############################################################################

1. attention可视化我觉得不需要变形到多个姿势测试效果，只需要在当前输入图片的姿势和视角下即可，为了看到不可见区域的可视化，需要在当前姿势的跟关节变形让手绕x轴渲染90度 （np.array([-np.pi/2, 0, 0])）
2. 也进行local的操作，
3. 我发现现在只保留12337个点看上去效果特别不好，有很多的空洞，我们需要恢复其他致密化的点（我已经把代码部分打开了），那么针对这些点如何赋值呢？我需要你分析一个合适的方案然后操作，在当前聊天中分析并询问我

############################################################################

global图还可以，是我们理想中的效果，输入视角的颜色偏蓝，表示global的依赖低，而手心的不可见部分则global高。但是local部分的图完全不是我们理想的效果，我需要你给我分析，并解释大片的绿色是什么含义。
或者你可以直接离线统计这个local的数值，缓存下来方便我们分析


############################################################################

我现在需要你详细解释这个依赖度是如何衡量的

############################################################################

我现在需要你给我另外一个可视化部分的分析和思考，当前的attention可视化是计算的点图交叉注意力之后，3D空间中每个点的情况。我现在需要回顾针对8个采样点进行attention可视化部分的逻辑。
这里的一个最重要的问题就是需要从VET在投影回真正的image，VET的构造在代码 modeling_hand_lrm.py 1913-1928行。深入挖掘分析这里的构造方式，以及如何反投影回图像空间。

image-level evidence explanation
把点对 VET 的依赖进一步 rollout 回：
点对应哪些点局部 patch
patch 对应哪些 image 坐标
最后再 rasterize 成 image heatmap。
只有第二类图，才能回答“attention 在图像上到底有没有看对地方”


############################################################################

考虑到这一段的逻辑和操作非常复杂，我需要你严格分析这里的代码逻辑，然后实现针对当前采样的8个点，直接在VET上的attention可视化，以及反映射回 image 上的 attention可视化。
尽量小的入侵之前的原始代码，可以使用接口，



############################################################################

还有一个可视化，针对我们随机采样的8个点，我需要可视化出来他在输入图片的姿势下对应的位置。具体来说，针对没有采样到的点，用默认的点云表示，即输入高斯渲染器的时候，让其只保留位置信息，颜色属性为默认的灰色，不透明度为默认的1，scaling和rotation都是默认的。而对采样到的点，则放大他们的scaling以凸显出来，并且颜色改为红色以更好地凸显。


############################################################################

先直接跑一遍 only-eval，把生成结果目录和图的含义逐张解释出来。
<!-- 把 image rollout 再细化成更严格的 transformer rollout，而不是现在的最后一层 cross-attn 近似。 -->

############################################################################